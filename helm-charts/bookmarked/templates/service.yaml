apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookmarked.fullname" . }}-server
  labels:
    app: {{ include "bookmarked.name" . }}-server
    chart: {{ include "bookmarked.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.server.type }}
  ports:
    - port: {{ .Values.service.server.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.server.type "NodePort") .Values.service.server.nodePort }}
      nodePort: {{ .Values.service.server.nodePort }}
      {{- end }}
  selector:
    app: {{ include "bookmarked.name" . }}-server
    release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookmarked.fullname" . }}-client
  labels:
    app: {{ include "bookmarked.name" . }}-client
    chart: {{ include "bookmarked.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.client.type }}
  ports:
    - port: {{ .Values.service.client.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.client.type "NodePort") .Values.service.client.nodePort }}
      nodePort: {{ .Values.service.client.nodePort }}
      {{- end }}
  selector:
    app: {{ include "bookmarked.name" . }}-client
    release: {{ .Release.Name }}
