apiVersion: v1
kind: Secret
metadata:
  name: {{ include "intervalai.fullname" . }}-secret
  labels:
    app: {{ include "intervalai.name" . }}
    chart: {{ include "intervalai.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $value := .Values.secret.data }}
  {{ $key }}: {{ $value }}
  {{- end }}
