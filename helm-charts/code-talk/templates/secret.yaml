apiVersion: v1
kind: Secret
metadata:
  name: {{ include "code-talk.fullname" . }}-secret
  labels:
    app: {{ include "code-talk.name" . }}
    chart: {{ include "code-talk.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $value := .Values.secret.data }}
  {{ $key }}: {{ $value }}
  {{- end }}
