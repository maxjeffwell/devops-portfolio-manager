apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "educationelly-graphql.fullname" . }}-client
  labels:
    app: {{ include "educationelly-graphql.name" . }}-client
    {{- include "educationelly-graphql.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "educationelly-graphql.name" . }}-client
      {{- include "educationelly-graphql.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: {{ include "educationelly-graphql.name" . }}-client
        {{- include "educationelly-graphql.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "educationelly-graphql.serviceAccountName" . }}
      containers:
      - name: client
        image: "{{ .Values.image.client.repository }}:{{ .Values.image.client.tag }}"
        imagePullPolicy: {{ .Values.image.client.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.client.targetPort }}
          protocol: TCP
        env:
        {{- range .Values.env.client }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        resources:
          {{- toYaml .Values.resources.client | nindent 12 }}
