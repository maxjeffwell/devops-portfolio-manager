---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radar-tailnet-tls
  namespace: radar
spec:
  secretName: radar-tailnet-tls
  dnsNames:
    - radar.tailnet.el-jefe.me
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod-dns
