PORT=5001
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
# In production, restrict to your specific frontend domains
# Examples:
#   Development: http://localhost:3000,http://localhost:31256
#   Production: https://devops.example.com,https://dashboard.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:31256

# ArgoCD Configuration
ARGOCD_SERVER=argocd-server.argocd.svc.cluster.local
ARGOCD_USERNAME=admin
ARGOCD_PASSWORD=Da4YDcDFyOzbM9JF

# Prometheus Configuration
PROMETHEUS_URL=http://prometheus-kube-prometheus-prometheus.monitoring.svc.cluster.local:9090

# GitHub Configuration (optional - for triggering workflows)
GITHUB_TOKEN=
GITHUB_OWNER=maxjeffwell
GITHUB_REPO=devops-portfolio-manager

# Kubernetes Configuration
KUBECONFIG_PATH=
