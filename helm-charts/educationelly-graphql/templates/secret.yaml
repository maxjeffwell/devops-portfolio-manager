apiVersion: v1
kind: Secret
metadata:
  name: {{ include "educationelly-graphql.fullname" . }}-secret
  labels:
    app: {{ include "educationelly-graphql.name" . }}
    chart: {{ include "educationelly-graphql.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $value := .Values.secret.data }}
  {{ $key }}: {{ $value }}
  {{- end }}
