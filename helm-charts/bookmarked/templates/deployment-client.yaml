apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bookmarked.fullname" . }}-client
  labels:
    app: {{ include "bookmarked.name" . }}-client
    {{- include "bookmarked.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "bookmarked.name" . }}-client
      {{- include "bookmarked.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: {{ include "bookmarked.name" . }}-client
        {{- include "bookmarked.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "bookmarked.serviceAccountName" . }}
      securityContext: {}
      containers:
      - name: client
        securityContext:
          runAsNonRoot: false
          runAsUser: 0
        image: "{{ .Values.image.client.repository }}:{{ .Values.image.client.tag }}"
        imagePullPolicy: {{ .Values.image.client.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.client.targetPort }}
          protocol: TCP
        env:
        {{- range .Values.env.client }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        resources:
          {{- toYaml .Values.resources.client | nindent 12 }}
