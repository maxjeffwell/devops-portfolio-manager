apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: percona-mongodb
# PerconaServerMongoDB clusters only. The operator, the percona-mongodb namespace,
# secrets (users, garage-s3-backup, TLS) and NetworkPolicies are NOT in this tree.
resources:
  - mongodb-educationelly.yaml
  - mongodb-educationelly-graphql.yaml
  - mongodb-intervalai.yaml
