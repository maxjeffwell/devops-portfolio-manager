replicaCount: 2
image:
  api:
    repository: maxjeffwell/educationelly-api
    tag: 20251203-123819-87990d2
    pullPolicy: Always
  client:
    repository: maxjeffwell/educationelly-client
    tag: 20251203-123819-87990d2
    pullPolicy: Always
service:
  api:
    type: ClusterIP
    port: 8080
    targetPort: 8080
    nodePort: 30007
  client:
    type: NodePort
    port: 80
    targetPort: 3000
    nodePort: 30008
resources:
  api:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi
  client:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
env:
  api:
    - name: NODE_ENV
      value: "production"
    - name: PORT
      value: "8080"
    - name: MONGODB_URI
      valueFrom:
        secretKeyRef:
          name: educationelly-secret
          key: MONGODB_URI
    - name: JWT_SECRET
      valueFrom:
        secretKeyRef:
          name: educationelly-secret
          key: JWT_SECRET
  client:
    - name: REACT_APP_API_URL
      value: "http://educationelly-api:8080"
serviceAccount:
  create: true
podSecurityContext:
  fsGroup: 2000
securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  runAsUser: 1001
livenessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 30
  periodSeconds: 10
readinessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5

# Cloud Database Configuration
# IMPORTANT: Replace these with your actual cloud database credentials
# These are placeholder values - update before deployment
secret:
  data:
    # MongoDB Atlas connection string (base64 encoded)
    # Example: mongodb+srv://<username>:<password>@cluster.mongodb.net/educationelly
    MONGODB_URI: "REPLACE_WITH_YOUR_MONGODB_ATLAS_URI"
    # JWT Secret for authentication (base64 encoded)
    JWT_SECRET: "REPLACE_WITH_YOUR_JWT_SECRET"
