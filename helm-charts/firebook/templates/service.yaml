apiVersion: v1
kind: Service
metadata:
  name: {{ include "firebook.fullname" . }}-api
  labels:
    app: {{ include "firebook.name" . }}-api
    chart: {{ include "firebook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.api.type }}
  ports:
    - port: {{ .Values.service.api.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.api.type "NodePort") .Values.service.api.nodePort }}
      nodePort: {{ .Values.service.api.nodePort }}
      {{- end }}
  selector:
    app: {{ include "firebook.name" . }}-api
    release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "firebook.fullname" . }}-client
  labels:
    app: {{ include "firebook.name" . }}-client
    chart: {{ include "firebook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.client.type }}
  ports:
    - port: {{ .Values.service.client.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.client.type "NodePort") .Values.service.client.nodePort }}
      nodePort: {{ .Values.service.client.nodePort }}
      {{- end }}
  selector:
    app: {{ include "firebook.name" . }}-client
    release: {{ .Release.Name }}
